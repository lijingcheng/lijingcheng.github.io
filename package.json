{
  "name": "blog",
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/gen-image-manifest.mjs && vitepress dev",
    "build": "node scripts/gen-image-manifest.mjs && vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "^1.0.0"
  }
}